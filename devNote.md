add:
  make button popups (hover)
  shift+enter should add new line instead of sending message (add textarea vertical increment and scrollbar for textarea)
  brower icon animation whilte bot is typing